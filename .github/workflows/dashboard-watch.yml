name: Dashboard Watchdog

on:
  workflow_dispatch:

concurrency:
  group: dashboard-watch-feature
  cancel-in-progress: true

jobs:
  deep-dashboard-check:
    name: Deep Dashboard Check
    uses: ./.github/workflows/dashboard-tests.yml
    with:
      ref: feature/mcp-mvp
